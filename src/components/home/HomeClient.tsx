'use client';

import React from 'react';
import { useTranslations } from 'next-intl';

export default function HomeClient() {
    const t = useTranslations('Home');

    return null; // Add any client-side only functionality here
}